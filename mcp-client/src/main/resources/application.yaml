server:
  port: 8004

spring:
  profiles:
    active: streamable
  application:
    name: mcp-client
  ai:
    mcp:
      client:
        enabled: true
        name: my-mcp-client
        version: 1.0.0
        request-timeout: 60s
        type: SYNC
